import React from 'react';
import {View,Text} from 'react-native'
import Map from './Map';

const Home = () => {
    return (
        <View style={{flex: 1}}>
            <Map/>
        </View>
    );
};

export default Home;
